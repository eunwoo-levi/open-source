# 환경 변수 예시 파일 (.env.example)
# 실제 사용 시 이 파일을 .env 로 복사한 뒤 값을 채워주세요.
# $ cp .env.example .env

# Flask 비밀 키 — 운영 환경에서는 충분히 복잡한 임의 문자열을 사용하세요.
SECRET_KEY=your-secure-secret-key-here

# 데이터베이스 URL (기본값: SQLite)
# 운영 환경에서는 PostgreSQL 등으로 교체 가능합니다.
DATABASE_URL=sqlite:///journal.db
